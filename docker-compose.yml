# Nextcloud MCP Server - Docker Compose
# For self-hosting on VPS at mcp.techmavie.digital/nextcloud

services:
  mcp-nextcloud:
    build: .
    container_name: mcp-nextcloud
    restart: unless-stopped
    ports:
      - "8085:8080"
    environment:
      - PORT=8080
      - HOST=0.0.0.0
      - NODE_ENV=production
      - ANALYTICS_FILE=/app/data/analytics.json
      - MCP_API_KEY=${MCP_API_KEY:-}
      - KEY_SERVICE_URL=${KEY_SERVICE_URL:-}
      - KEY_SERVICE_TOKEN=${KEY_SERVICE_TOKEN:-}
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS:-https://smithery.ai,https://claude.ai}
    volumes:
      - analytics-data:/app/data
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - mcp-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  mcp-network:
    driver: bridge

volumes:
  analytics-data:
